import { ReactElement, ReactNode } from 'react';
import { SxProps, Theme } from '@mui/material/styles';
export interface ListingTableRowActionsProps {
    /**
     * Action buttons or elements
     */
    children: ReactNode;
    /**
     * Whether actions are always visible or only on row hover
     * @default 'always'
     */
    visibility?: 'always' | 'hover';
    /**
     * Alignment of action buttons
     * @default 'right'
     */
    align?: 'left' | 'center' | 'right';
    /**
     * Custom styles for the container
     */
    sx?: SxProps<Theme>;
}
export declare function ListingTableRowActions({ children, visibility, align, sx, }: ListingTableRowActionsProps): ReactElement;
export default ListingTableRowActions;
