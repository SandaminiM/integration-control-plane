import { ReactElement, ReactNode } from 'react';
import { SxProps, Theme } from '@mui/material/styles';
export interface ListingTableCellIconProps {
    /**
     * Icon element to display
     */
    icon: ReactNode;
    /**
     * Primary text content
     */
    primary: string;
    /**
     * Secondary text content (optional)
     */
    secondary?: string;
    /**
     * Icon position relative to text
     * @default 'left'
     */
    iconPosition?: 'left' | 'right';
    /**
     * Custom styles for the icon wrapper
     */
    iconSx?: SxProps<Theme>;
    /**
     * Custom styles for the container
     */
    sx?: SxProps<Theme>;
}
export declare function ListingTableCellIcon({ icon, primary, secondary, iconPosition, iconSx, sx, }: ListingTableCellIconProps): ReactElement;
export default ListingTableCellIcon;
