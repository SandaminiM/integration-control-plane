"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var index_exports = {};
__export(index_exports, {
  Area: () => import_recharts7.Area,
  AreaChart: () => AreaChart_default,
  Bar: () => import_recharts7.Bar,
  BarChart: () => BarChart_default,
  CartesianGrid: () => import_recharts7.CartesianGrid,
  Cell: () => import_recharts7.Cell,
  ChartTooltip: () => import_recharts7.Tooltip,
  Label: () => import_recharts7.Label,
  LabelList: () => import_recharts7.LabelList,
  Legend: () => import_recharts7.Legend,
  Line: () => import_recharts7.Line,
  LineChart: () => LineChart_default,
  Pie: () => import_recharts7.Pie,
  PieChart: () => PieChart_default,
  PolarAngleAxis: () => import_recharts7.PolarAngleAxis,
  PolarGrid: () => import_recharts7.PolarGrid,
  PolarRadiusAxis: () => import_recharts7.PolarRadiusAxis,
  Radar: () => import_recharts7.Radar,
  RadarChart: () => RadarChart_default,
  RadialBar: () => import_recharts7.RadialBar,
  RadialBarChart: () => RadialBarChart_default,
  ResponsiveContainer: () => import_recharts7.ResponsiveContainer,
  Scatter: () => import_recharts7.Scatter,
  XAxis: () => import_recharts7.XAxis,
  YAxis: () => import_recharts7.YAxis,
  ZAxis: () => import_recharts7.ZAxis
});
module.exports = __toCommonJS(index_exports);

// src/components/charts/BarChart/BarChart.tsx
var import_react = __toESM(require("react"), 1);
var import_oxygen_ui = require("@wso2/oxygen-ui");
var import_recharts = require("recharts");
var import_jsx_runtime = require("react/jsx-runtime");
var BarChart = ({
  data,
  bars,
  xAxisDataKey,
  height = 300,
  width = "100%",
  legend,
  grid,
  tooltip,
  layout = "horizontal",
  margin,
  barCategoryGap,
  barGap,
  barSize,
  maxBarSize,
  stackOffset = "none",
  accessibilityLayer = true,
  xAxis,
  yAxis,
  isAnimationActive = true,
  animationDuration = 1500,
  animationBegin = 0,
  animationEasing = "ease",
  colors: customColors,
  children,
  onClick,
  onMouseEnter,
  onMouseLeave,
  onMouseDown,
  onMouseUp,
  onMouseMove
}) => {
  const theme = (0, import_oxygen_ui.useTheme)();
  const { mode, systemMode } = (0, import_oxygen_ui.useColorScheme)();
  const isDark = mode === "dark" || mode === "system" && systemMode === "dark";
  const colors = import_react.default.useMemo(() => {
    const defaultColors = {
      primary: isDark ? "#F87643" : "#fa7b3f",
      background: isDark ? "#1e1e1e" : "#f5f5f5",
      text: isDark ? "#d4d4d4" : "#24292e",
      comment: isDark ? "#6a9955" : "#6a737d",
      keyword: isDark ? "#569cd6" : "#d73a49",
      string: isDark ? "#ce9178" : "#032f62",
      function: isDark ? "#dcdcaa" : "#6f42c1",
      number: isDark ? "#b5cea8" : "#005cc5",
      operator: isDark ? "#d4d4d4" : "#d73a49"
    };
    const themeSyntax = isDark ? theme.vars?.syntax?.dark : theme.vars?.syntax?.light;
    return {
      ...defaultColors,
      ...themeSyntax,
      primary: theme.palette.primary?.main || themeSyntax?.primary || defaultColors.primary
    };
  }, [isDark, theme.palette.primary?.main, theme.vars]);
  const barColors = import_react.default.useMemo(() => {
    if (customColors && customColors.length > 0) return customColors;
    return [
      colors.primary,
      colors.keyword,
      colors.string,
      colors.function,
      colors.number,
      colors.operator
    ];
  }, [
    customColors,
    colors.primary,
    colors.keyword,
    colors.string,
    colors.function,
    colors.number,
    colors.operator
  ]);
  const legendConfig = import_react.default.useMemo(
    () => ({ show: true, align: "center", verticalAlign: "bottom", ...legend }),
    [legend]
  );
  const gridConfig = import_react.default.useMemo(() => ({ show: true, strokeDasharray: "3 3", ...grid }), [grid]);
  const marginConfig = import_react.default.useMemo(
    () => ({ top: 12, right: 24, left: 24, bottom: 40, ...margin }),
    [margin]
  );
  const xAxisConfig = import_react.default.useMemo(() => ({ show: true, ...xAxis }), [xAxis]);
  const yAxisConfig = import_react.default.useMemo(() => ({ show: true, ...yAxis }), [yAxis]);
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_recharts.ResponsiveContainer, { width, height, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(
    import_recharts.BarChart,
    {
      data,
      layout,
      margin: marginConfig,
      barCategoryGap,
      barGap,
      barSize,
      maxBarSize,
      stackOffset,
      accessibilityLayer,
      children: [
        gridConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          import_recharts.CartesianGrid,
          {
            strokeDasharray: gridConfig.strokeDasharray,
            stroke: colors.text,
            vertical: false
          }
        ),
        xAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          import_recharts.XAxis,
          {
            type: layout === "vertical" ? "number" : "category",
            dataKey: layout === "vertical" ? void 0 : xAxisDataKey,
            stroke: colors.text,
            tick: {
              fill: colors.text,
              fontSize: 12
            },
            tickMargin: 8,
            axisLine: { stroke: colors.text },
            tickLine: { stroke: colors.text },
            label: xAxisConfig.name ? {
              value: xAxisConfig.name,
              position: "insideBottom",
              offset: -18,
              fill: colors.text,
              fontSize: 12
            } : void 0
          }
        ),
        yAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          import_recharts.YAxis,
          {
            type: layout === "vertical" ? "category" : "number",
            dataKey: layout === "vertical" ? xAxisDataKey : void 0,
            stroke: colors.text,
            tick: {
              fill: colors.text,
              fontSize: 12
            },
            tickMargin: 8,
            axisLine: { stroke: colors.text },
            tickLine: { stroke: colors.text },
            label: yAxisConfig.name ? {
              value: yAxisConfig.name,
              angle: -90,
              position: "insideLeft",
              offset: 0,
              fill: colors.text,
              fontSize: 12
            } : void 0
          }
        ),
        tooltip?.show !== false && (() => {
          const { contentStyle, itemStyle, labelStyle, show, ...rest } = tooltip || {};
          return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
            import_recharts.Tooltip,
            {
              cursor: {
                fill: isDark ? "rgba(255, 255, 255, 0.05)" : "rgba(0, 0, 0, 0.05)"
              },
              ...rest,
              contentStyle: {
                backgroundColor: colors.background,
                border: `1px solid ${colors.text}`,
                borderRadius: theme.shape.borderRadius ?? 8,
                color: colors.text,
                boxShadow: "0 4px 6px -1px rgb(0 0 0 / 0.1)",
                ...contentStyle
              },
              itemStyle: {
                color: colors.text,
                fontSize: 12,
                ...itemStyle
              },
              labelStyle: {
                color: colors.text,
                fontWeight: 600,
                marginBottom: 4,
                ...labelStyle
              }
            }
          );
        })(),
        legendConfig.show && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          import_recharts.Legend,
          {
            align: legendConfig.align,
            verticalAlign: legendConfig.verticalAlign,
            iconType: "circle",
            formatter: (value) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
              "span",
              {
                style: {
                  color: colors.text,
                  fontSize: 13,
                  fontWeight: 500
                },
                children: value
              }
            ),
            wrapperStyle: { paddingTop: 32 }
          }
        ),
        bars?.map((bar, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          import_recharts.Bar,
          {
            dataKey: bar.dataKey,
            name: bar.name,
            fill: bar.fill || barColors[index % barColors.length],
            radius: bar.radius ?? [4, 4, 0, 0],
            stackId: bar.stackId,
            hide: bar.hide,
            label: bar.label,
            isAnimationActive: bar.isAnimationActive ?? isAnimationActive,
            animationDuration: bar.animationDuration ?? animationDuration,
            animationBegin: bar.animationBegin ?? animationBegin,
            animationEasing: bar.animationEasing ?? animationEasing,
            onClick: bar.onClick || onClick,
            onMouseEnter: bar.onMouseEnter || onMouseEnter,
            onMouseLeave: bar.onMouseLeave || onMouseLeave,
            onMouseDown: bar.onMouseDown || onMouseDown,
            onMouseUp: bar.onMouseUp || onMouseUp,
            onMouseMove: bar.onMouseMove || onMouseMove
          },
          `${bar.dataKey}-${index}`
        )),
        children
      ]
    }
  ) });
};
BarChart.displayName = "BarChart";
var BarChart_default = BarChart;

// src/components/charts/PieChart/PieChart.tsx
var import_react2 = __toESM(require("react"), 1);
var import_oxygen_ui2 = require("@wso2/oxygen-ui");
var import_recharts2 = require("recharts");
var import_jsx_runtime2 = require("react/jsx-runtime");
var PieChart = ({
  data = [],
  pies,
  nameKey,
  height = 300,
  width = "100%",
  legend,
  tooltip,
  margin,
  cx = "50%",
  cy = "50%",
  startAngle = 0,
  endAngle = 360,
  innerRadius = 0,
  outerRadius = "80%",
  paddingAngle = 0,
  cornerRadius = 0,
  minAngle = 0,
  isAnimationActive = true,
  animationDuration = 1500,
  animationBegin = 0,
  animationEasing = "ease",
  colors: customColors,
  label,
  labelLine,
  onClick,
  onMouseEnter,
  onMouseLeave,
  onMouseDown,
  onMouseUp,
  onMouseMove,
  onContextMenu,
  onDoubleClick,
  children
}) => {
  const theme = (0, import_oxygen_ui2.useTheme)();
  const { mode, systemMode } = (0, import_oxygen_ui2.useColorScheme)();
  const isDark = mode === "dark" || mode === "system" && systemMode === "dark";
  const syntaxColors = import_react2.default.useMemo(() => {
    const defaultColors = {
      primary: isDark ? "#F87643" : "#fa7b3f",
      background: isDark ? "#1e1e1e" : "#f5f5f5",
      text: isDark ? "#d4d4d4" : "#24292e",
      comment: isDark ? "#6a9955" : "#6a737d",
      keyword: isDark ? "#569cd6" : "#d73a49",
      string: isDark ? "#ce9178" : "#032f62",
      function: isDark ? "#dcdcaa" : "#6f42c1",
      number: isDark ? "#b5cea8" : "#005cc5",
      operator: isDark ? "#d4d4d4" : "#d73a49"
    };
    const themeSyntax = isDark ? theme.vars?.syntax?.dark : theme.vars?.syntax?.light;
    return {
      ...defaultColors,
      ...themeSyntax,
      primary: theme.palette.primary?.main || themeSyntax?.primary || defaultColors.primary
    };
  }, [isDark, theme.palette.primary?.main, theme.vars]);
  const chartColors = import_react2.default.useMemo(() => {
    if (customColors && customColors.length > 0) return customColors;
    return [
      syntaxColors.primary,
      syntaxColors.keyword,
      syntaxColors.string,
      syntaxColors.function,
      syntaxColors.number,
      syntaxColors.operator
    ];
  }, [
    customColors,
    syntaxColors.primary,
    syntaxColors.keyword,
    syntaxColors.string,
    syntaxColors.function,
    syntaxColors.number,
    syntaxColors.operator
  ]);
  const legendConfig = import_react2.default.useMemo(
    () => ({ show: true, align: "center", verticalAlign: "bottom", ...legend }),
    [legend]
  );
  const marginConfig = import_react2.default.useMemo(
    () => ({ top: 12, right: 24, left: 24, bottom: 40, ...margin }),
    [margin]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(import_recharts2.ResponsiveContainer, { width, height, children: /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(import_recharts2.PieChart, { data, margin: marginConfig, children: [
    pies?.map((pie, index) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
      import_recharts2.Pie,
      {
        data,
        dataKey: pie.dataKey,
        nameKey: pie.nameKey || nameKey,
        name: pie.name,
        cx: pie.cx ?? cx,
        cy: pie.cy ?? cy,
        startAngle: pie.startAngle ?? startAngle,
        endAngle: pie.endAngle ?? endAngle,
        innerRadius: pie.innerRadius ?? innerRadius,
        outerRadius: pie.outerRadius ?? outerRadius,
        paddingAngle: pie.paddingAngle ?? paddingAngle,
        cornerRadius: pie.cornerRadius ?? cornerRadius,
        minAngle: pie.minAngle ?? minAngle,
        isAnimationActive: pie.isAnimationActive ?? isAnimationActive,
        animationDuration: pie.animationDuration ?? animationDuration,
        animationBegin: pie.animationBegin ?? animationBegin,
        animationEasing: pie.animationEasing ?? animationEasing,
        label: pie.label ?? label,
        labelLine: pie.labelLine ?? labelLine,
        onClick: pie.onClick || onClick,
        onMouseEnter: pie.onMouseEnter || onMouseEnter,
        onMouseLeave: pie.onMouseLeave || onMouseLeave,
        onMouseDown: pie.onMouseDown || onMouseDown,
        onMouseUp: pie.onMouseUp || onMouseUp,
        onMouseMove: pie.onMouseMove || onMouseMove,
        onContextMenu: pie.onContextMenu || onContextMenu,
        onDoubleClick: pie.onDoubleClick || onDoubleClick,
        children: data.map((entry, index2) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(import_recharts2.Cell, { fill: chartColors[index2 % chartColors.length] }, `cell-${index2}`))
      },
      `${pie.dataKey}-${index}`
    )),
    tooltip?.show !== false && (() => {
      const { contentStyle, itemStyle, labelStyle, show, ...rest } = tooltip || {};
      return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
        import_recharts2.Tooltip,
        {
          ...rest,
          contentStyle: {
            backgroundColor: syntaxColors.background,
            border: `1px solid ${syntaxColors.text}`,
            borderRadius: theme.shape.borderRadius ?? 8,
            color: syntaxColors.text,
            boxShadow: "0 4px 6px -1px rgb(0 0 0 / 0.1)",
            ...contentStyle
          },
          itemStyle: {
            color: syntaxColors.text,
            fontSize: 12,
            ...itemStyle
          },
          labelStyle: {
            color: syntaxColors.text,
            fontWeight: 600,
            marginBottom: 4,
            ...labelStyle
          }
        }
      );
    })(),
    legendConfig.show && /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
      import_recharts2.Legend,
      {
        align: legendConfig.align,
        verticalAlign: legendConfig.verticalAlign,
        iconType: "circle",
        formatter: (value) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
          "span",
          {
            style: {
              color: syntaxColors.text,
              fontSize: 13,
              fontWeight: 500
            },
            children: value
          }
        ),
        wrapperStyle: { paddingTop: 32 }
      }
    ),
    children
  ] }) });
};
PieChart.displayName = "PieChart";
var PieChart_default = PieChart;

// src/components/charts/LineChart/LineChart.tsx
var import_react3 = __toESM(require("react"), 1);
var import_oxygen_ui3 = require("@wso2/oxygen-ui");
var import_recharts3 = require("recharts");
var import_jsx_runtime3 = require("react/jsx-runtime");
var LineChart = ({
  data,
  lines,
  xAxisDataKey,
  height = 300,
  width = "100%",
  legend,
  grid,
  tooltip,
  layout = "horizontal",
  margin,
  syncId,
  syncMethod,
  role,
  title,
  desc,
  accessibilityLayer = true,
  xAxis,
  yAxis,
  onClick,
  onMouseDown,
  onMouseUp,
  onMouseMove,
  onMouseEnter,
  onMouseLeave,
  isAnimationActive = true,
  animationDuration = 1500,
  animationBegin = 0,
  animationEasing = "ease",
  colors: customColors,
  children
}) => {
  const theme = (0, import_oxygen_ui3.useTheme)();
  const { mode, systemMode } = (0, import_oxygen_ui3.useColorScheme)();
  const isDark = mode === "dark" || mode === "system" && systemMode === "dark";
  const colors = import_react3.default.useMemo(() => {
    const defaultColors = {
      primary: isDark ? "#F87643" : "#fa7b3f",
      background: isDark ? "#1e1e1e" : "#f5f5f5",
      text: isDark ? "#d4d4d4" : "#24292e",
      comment: isDark ? "#6a9955" : "#6a737d",
      keyword: isDark ? "#569cd6" : "#d73a49",
      string: isDark ? "#ce9178" : "#032f62",
      function: isDark ? "#dcdcaa" : "#6f42c1",
      number: isDark ? "#b5cea8" : "#005cc5",
      operator: isDark ? "#d4d4d4" : "#d73a49"
    };
    const themeSyntax = isDark ? theme.vars?.syntax?.dark : theme.vars?.syntax?.light;
    return {
      ...defaultColors,
      ...themeSyntax,
      primary: theme.palette.primary?.main || themeSyntax?.primary || defaultColors.primary
    };
  }, [isDark, theme.palette.primary?.main, theme.vars]);
  const lineColors = import_react3.default.useMemo(() => {
    if (customColors && customColors.length > 0) return customColors;
    return [
      colors.primary,
      colors.keyword,
      colors.string,
      colors.function,
      colors.number,
      colors.operator
    ];
  }, [
    customColors,
    colors.primary,
    colors.keyword,
    colors.string,
    colors.function,
    colors.number,
    colors.operator
  ]);
  const legendConfig = import_react3.default.useMemo(
    () => ({ show: true, align: "center", verticalAlign: "bottom", ...legend }),
    [legend]
  );
  const gridConfig = import_react3.default.useMemo(() => ({ show: true, strokeDasharray: "3 3", ...grid }), [grid]);
  const marginConfig = import_react3.default.useMemo(
    () => ({ top: 12, right: 24, left: 24, bottom: 40, ...margin }),
    [margin]
  );
  const xAxisConfig = import_react3.default.useMemo(() => ({ show: true, ...xAxis }), [xAxis]);
  const yAxisConfig = import_react3.default.useMemo(() => ({ show: true, ...yAxis }), [yAxis]);
  return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(import_recharts3.ResponsiveContainer, { width, height, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)(
    import_recharts3.LineChart,
    {
      data,
      layout,
      margin: marginConfig,
      syncId,
      syncMethod,
      role,
      title,
      desc,
      accessibilityLayer,
      onClick,
      onMouseDown,
      onMouseUp,
      onMouseMove,
      onMouseEnter,
      onMouseLeave,
      children: [
        gridConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          import_recharts3.CartesianGrid,
          {
            strokeDasharray: gridConfig.strokeDasharray,
            stroke: colors.text,
            vertical: false
          }
        ),
        xAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          import_recharts3.XAxis,
          {
            type: xAxisConfig.type || (layout === "vertical" ? "number" : "category"),
            dataKey: layout === "vertical" ? void 0 : xAxisDataKey,
            stroke: colors.text,
            domain: xAxisConfig.domain,
            tickCount: xAxisConfig.tickCount,
            interval: xAxisConfig.interval,
            tick: {
              fill: colors.text,
              fontSize: 12
            },
            tickMargin: 8,
            axisLine: { stroke: colors.text },
            tickLine: { stroke: colors.text },
            label: xAxisConfig.name ? {
              value: xAxisConfig.name,
              position: "insideBottom",
              offset: -18,
              fill: colors.text,
              fontSize: 12
            } : void 0
          }
        ),
        yAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          import_recharts3.YAxis,
          {
            type: yAxisConfig.type || (layout === "vertical" ? "category" : "number"),
            dataKey: layout === "vertical" ? xAxisDataKey : void 0,
            stroke: colors.text,
            domain: yAxisConfig.domain,
            tickCount: yAxisConfig.tickCount,
            interval: yAxisConfig.interval,
            tick: {
              fill: colors.text,
              fontSize: 12
            },
            tickMargin: 8,
            axisLine: { stroke: colors.text },
            tickLine: { stroke: colors.text },
            label: yAxisConfig.name ? {
              value: yAxisConfig.name,
              angle: -90,
              position: "insideLeft",
              offset: 0,
              fill: colors.text,
              fontSize: 12
            } : void 0
          }
        ),
        tooltip?.show !== false && (() => {
          const { contentStyle, itemStyle, labelStyle, show, ...rest } = tooltip || {};
          return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
            import_recharts3.Tooltip,
            {
              ...rest,
              contentStyle: {
                backgroundColor: colors.background,
                border: `1px solid ${colors.text}`,
                borderRadius: theme.shape.borderRadius ?? 8,
                color: colors.text,
                boxShadow: "0 4px 6px -1px rgb(0 0 0 / 0.1)",
                ...contentStyle
              },
              itemStyle: {
                color: colors.text,
                fontSize: 12,
                ...itemStyle
              },
              labelStyle: {
                color: colors.text,
                fontWeight: 600,
                marginBottom: 4,
                ...labelStyle
              }
            }
          );
        })(),
        legendConfig.show && /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          import_recharts3.Legend,
          {
            align: legendConfig.align,
            verticalAlign: legendConfig.verticalAlign,
            iconType: "circle",
            formatter: (value) => /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
              "span",
              {
                style: {
                  color: colors.text,
                  fontSize: 13,
                  fontWeight: 500
                },
                children: value
              }
            ),
            wrapperStyle: { paddingTop: 32 }
          }
        ),
        lines?.map((line, index) => /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          import_recharts3.Line,
          {
            dataKey: line.dataKey,
            name: line.name,
            stroke: line.stroke || lineColors[index % lineColors.length],
            strokeWidth: line.strokeWidth ?? 2,
            strokeDasharray: line.strokeDasharray,
            type: line.type || "monotone",
            hide: line.hide,
            label: line.label,
            dot: line.dot ?? true,
            activeDot: line.activeDot ?? true,
            connectNulls: line.connectNulls,
            unit: line.unit,
            legendType: line.legendType || "line",
            onClick: line.onClick,
            onMouseDown: line.onMouseDown,
            onMouseUp: line.onMouseUp,
            onMouseMove: line.onMouseMove,
            onMouseOver: line.onMouseOver,
            onMouseOut: line.onMouseOut,
            onMouseEnter: line.onMouseEnter,
            onMouseLeave: line.onMouseLeave,
            isAnimationActive: line.isAnimationActive ?? isAnimationActive,
            animationDuration: line.animationDuration ?? animationDuration,
            animationBegin: line.animationBegin ?? animationBegin,
            animationEasing: line.animationEasing ?? animationEasing
          },
          `${line.dataKey}-${index}`
        )),
        children
      ]
    }
  ) });
};
LineChart.displayName = "LineChart";
var LineChart_default = LineChart;

// src/components/charts/AreaChart/AreaChart.tsx
var import_react4 = __toESM(require("react"), 1);
var import_oxygen_ui4 = require("@wso2/oxygen-ui");
var import_recharts4 = require("recharts");
var import_jsx_runtime4 = require("react/jsx-runtime");
var AreaChart = ({
  data,
  areas,
  xAxisDataKey,
  height = 300,
  width = "100%",
  legend,
  grid,
  tooltip,
  layout = "horizontal",
  margin,
  syncId,
  syncMethod,
  role,
  title,
  desc,
  accessibilityLayer = true,
  xAxis,
  yAxis,
  onClick,
  onMouseDown,
  onMouseUp,
  onMouseMove,
  onMouseEnter,
  onMouseLeave,
  isAnimationActive = true,
  animationDuration = 1500,
  animationBegin = 0,
  animationEasing = "ease",
  colors: customColors,
  children
}) => {
  const theme = (0, import_oxygen_ui4.useTheme)();
  const { mode, systemMode } = (0, import_oxygen_ui4.useColorScheme)();
  const isDark = mode === "dark" || mode === "system" && systemMode === "dark";
  const colors = import_react4.default.useMemo(() => {
    const defaultColors = {
      primary: isDark ? "#F87643" : "#fa7b3f",
      background: isDark ? "#1e1e1e" : "#f5f5f5",
      text: isDark ? "#d4d4d4" : "#24292e",
      comment: isDark ? "#6a9955" : "#6a737d",
      keyword: isDark ? "#569cd6" : "#d73a49",
      string: isDark ? "#ce9178" : "#032f62",
      function: isDark ? "#dcdcaa" : "#6f42c1",
      number: isDark ? "#b5cea8" : "#005cc5",
      operator: isDark ? "#d4d4d4" : "#d73a49"
    };
    const themeSyntax = isDark ? theme.vars?.syntax?.dark : theme.vars?.syntax?.light;
    return {
      ...defaultColors,
      ...themeSyntax,
      primary: theme.palette.primary?.main || themeSyntax?.primary || defaultColors.primary
    };
  }, [isDark, theme.palette.primary?.main, theme.vars]);
  const idPrefix = import_react4.default.useId();
  const areaColors = import_react4.default.useMemo(() => {
    if (customColors && customColors.length > 0) return customColors;
    return [
      colors.primary,
      colors.keyword,
      colors.string,
      colors.function,
      colors.number,
      colors.operator
    ];
  }, [
    customColors,
    colors.primary,
    colors.keyword,
    colors.string,
    colors.function,
    colors.number,
    colors.operator
  ]);
  const legendConfig = import_react4.default.useMemo(
    () => ({ show: true, align: "center", verticalAlign: "bottom", ...legend }),
    [legend]
  );
  const gridConfig = import_react4.default.useMemo(() => ({ show: true, strokeDasharray: "3 3", ...grid }), [grid]);
  const marginConfig = import_react4.default.useMemo(
    () => ({ top: 12, right: 24, left: 24, bottom: 40, ...margin }),
    [margin]
  );
  const xAxisConfig = import_react4.default.useMemo(() => ({ show: true, ...xAxis }), [xAxis]);
  const yAxisConfig = import_react4.default.useMemo(() => ({ show: true, ...yAxis }), [yAxis]);
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(import_recharts4.ResponsiveContainer, { width, height, children: /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(
    import_recharts4.AreaChart,
    {
      data,
      layout,
      margin: marginConfig,
      syncId,
      syncMethod,
      role,
      title,
      desc,
      accessibilityLayer,
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("defs", { children: areas?.map((area, index) => {
          const color = area.fill || area.stroke || areaColors[index % areaColors.length];
          return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(
            "linearGradient",
            {
              id: `${idPrefix}-gradient-${area.dataKey}`,
              x1: "0",
              y1: "0",
              x2: "0",
              y2: "1",
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("stop", { offset: "5%", stopColor: color, stopOpacity: area.fillOpacity ?? 0.8 }),
                /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("stop", { offset: "95%", stopColor: color, stopOpacity: 0 })
              ]
            },
            `${idPrefix}-gradient-${area.dataKey}`
          );
        }) }),
        gridConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
          import_recharts4.CartesianGrid,
          {
            strokeDasharray: gridConfig.strokeDasharray,
            stroke: colors.text,
            vertical: false
          }
        ),
        xAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
          import_recharts4.XAxis,
          {
            type: xAxisConfig.type || (layout === "vertical" ? "number" : "category"),
            dataKey: layout === "vertical" ? void 0 : xAxisDataKey,
            stroke: colors.text,
            domain: xAxisConfig.domain,
            tickCount: xAxisConfig.tickCount,
            interval: xAxisConfig.interval,
            tick: {
              fill: colors.text,
              fontSize: 12
            },
            tickMargin: 8,
            axisLine: { stroke: colors.text },
            tickLine: { stroke: colors.text },
            label: xAxisConfig.name ? {
              value: xAxisConfig.name,
              position: "insideBottom",
              offset: -18,
              fill: colors.text,
              fontSize: 12
            } : void 0
          }
        ),
        yAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
          import_recharts4.YAxis,
          {
            type: yAxisConfig.type || (layout === "vertical" ? "category" : "number"),
            dataKey: layout === "vertical" ? xAxisDataKey : void 0,
            stroke: colors.text,
            domain: yAxisConfig.domain,
            tickCount: yAxisConfig.tickCount,
            interval: yAxisConfig.interval,
            tick: {
              fill: colors.text,
              fontSize: 12
            },
            tickMargin: 8,
            axisLine: { stroke: colors.text },
            tickLine: { stroke: colors.text },
            label: yAxisConfig.name ? {
              value: yAxisConfig.name,
              angle: -90,
              position: "insideLeft",
              offset: 0,
              fill: colors.text,
              fontSize: 12
            } : void 0
          }
        ),
        tooltip?.show !== false && (() => {
          const { contentStyle, itemStyle, labelStyle, show, ...rest } = tooltip || {};
          return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
            import_recharts4.Tooltip,
            {
              ...rest,
              contentStyle: {
                backgroundColor: colors.background,
                border: `1px solid ${colors.text}`,
                borderRadius: theme.shape.borderRadius ?? 8,
                color: colors.text,
                boxShadow: "0 4px 6px -1px rgb(0 0 0 / 0.1)",
                ...contentStyle
              },
              itemStyle: {
                color: colors.text,
                fontSize: 12,
                ...itemStyle
              },
              labelStyle: {
                color: colors.text,
                fontWeight: 600,
                marginBottom: 4,
                ...labelStyle
              }
            }
          );
        })(),
        legendConfig.show && /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
          import_recharts4.Legend,
          {
            align: legendConfig.align,
            verticalAlign: legendConfig.verticalAlign,
            iconType: "circle",
            formatter: (value) => /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
              "span",
              {
                style: {
                  color: colors.text,
                  fontSize: 13,
                  fontWeight: 500
                },
                children: value
              }
            ),
            wrapperStyle: { paddingTop: 32 }
          }
        ),
        areas?.map((area, index) => /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
          import_recharts4.Area,
          {
            dataKey: area.dataKey,
            name: area.name,
            stroke: area.stroke || areaColors[index % areaColors.length],
            fill: `url(#${idPrefix}-gradient-${area.dataKey})`,
            strokeWidth: area.strokeWidth ?? 2,
            strokeDasharray: area.strokeDasharray,
            type: area.type || "monotone",
            stackId: area.stackId,
            hide: area.hide,
            label: area.label,
            dot: area.dot ?? true,
            activeDot: area.activeDot ?? true,
            connectNulls: area.connectNulls,
            unit: area.unit,
            legendType: area.legendType || "line",
            onClick: area.onClick || onClick,
            onMouseDown: area.onMouseDown || onMouseDown,
            onMouseUp: area.onMouseUp || onMouseUp,
            onMouseMove: area.onMouseMove || onMouseMove,
            onMouseOver: area.onMouseOver,
            onMouseOut: area.onMouseOut,
            onMouseEnter: area.onMouseEnter || onMouseEnter,
            onMouseLeave: area.onMouseLeave || onMouseLeave,
            isAnimationActive: area.isAnimationActive ?? isAnimationActive,
            animationDuration: area.animationDuration ?? animationDuration,
            animationBegin: area.animationBegin ?? animationBegin,
            animationEasing: area.animationEasing ?? animationEasing
          },
          `${area.dataKey}-${index}`
        )),
        children
      ]
    }
  ) });
};
AreaChart.displayName = "AreaChart";
var AreaChart_default = AreaChart;

// src/components/charts/RadarChart/RadarChart.tsx
var import_react5 = __toESM(require("react"), 1);
var import_oxygen_ui5 = require("@wso2/oxygen-ui");
var import_recharts5 = require("recharts");
var import_jsx_runtime5 = require("react/jsx-runtime");
var RadarChart = ({
  data,
  radars,
  radialBars,
  angleKey,
  height = 300,
  width = "100%",
  legend,
  tooltip,
  innerRadius = 0,
  outerRadius = "80%",
  cx = "50%",
  cy = "50%",
  startAngle = 90,
  endAngle = -270,
  margin,
  polarGrid,
  polarAngleAxis,
  polarRadiusAxis,
  syncId,
  syncMethod,
  role,
  title,
  desc,
  id,
  className,
  style,
  tabIndex,
  layout = "centric",
  stackOffset,
  reverseStackOrder,
  barSize,
  barCategoryGap,
  barGap,
  maxBarSize,
  onClick,
  onMouseDown,
  onMouseUp,
  onMouseMove,
  onMouseEnter,
  onMouseLeave,
  onContextMenu,
  onDoubleClick,
  accessibilityLayer = true,
  colors: customColors,
  children
}) => {
  const theme = (0, import_oxygen_ui5.useTheme)();
  const { mode, systemMode } = (0, import_oxygen_ui5.useColorScheme)();
  const isDark = mode === "dark" || mode === "system" && systemMode === "dark";
  const colors = import_react5.default.useMemo(() => {
    const defaultColors = {
      primary: isDark ? "#F87643" : "#fa7b3f",
      background: isDark ? "#1e1e1e" : "#f5f5f5",
      text: isDark ? "#d4d4d4" : "#24292e",
      comment: isDark ? "#6a9955" : "#6a737d",
      keyword: isDark ? "#569cd6" : "#d73a49",
      string: isDark ? "#ce9178" : "#032f62",
      function: isDark ? "#dcdcaa" : "#6f42c1",
      number: isDark ? "#b5cea8" : "#005cc5",
      operator: isDark ? "#d4d4d4" : "#d73a49"
    };
    const themeSyntax = isDark ? theme.vars?.syntax?.dark : theme.vars?.syntax?.light;
    return {
      ...defaultColors,
      ...themeSyntax,
      primary: theme.palette.primary?.main || themeSyntax?.primary || defaultColors.primary
    };
  }, [isDark, theme.palette.primary?.main, theme.vars]);
  const radarColors = import_react5.default.useMemo(() => {
    if (customColors && customColors.length > 0) return customColors;
    return [
      colors.primary,
      colors.keyword,
      colors.string,
      colors.function,
      colors.number,
      colors.operator
    ];
  }, [
    customColors,
    colors.primary,
    colors.keyword,
    colors.string,
    colors.function,
    colors.number,
    colors.operator
  ]);
  const legendConfig = import_react5.default.useMemo(
    () => ({ show: true, align: "center", verticalAlign: "bottom", ...legend }),
    [legend]
  );
  const marginConfig = import_react5.default.useMemo(
    () => ({ top: 20, right: 20, bottom: 20, left: 20, ...margin }),
    [margin]
  );
  const polarGridConfig = import_react5.default.useMemo(
    () => ({ show: true, gridType: "polygon", ...polarGrid }),
    [polarGrid]
  );
  const polarAngleAxisConfig = import_react5.default.useMemo(
    () => ({ show: true, ...polarAngleAxis }),
    [polarAngleAxis]
  );
  const polarRadiusAxisConfig = import_react5.default.useMemo(
    () => ({ show: false, ...polarRadiusAxis }),
    [polarRadiusAxis]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(import_recharts5.ResponsiveContainer, { width, height, children: /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)(
    import_recharts5.RadarChart,
    {
      data,
      cx,
      cy,
      innerRadius,
      outerRadius,
      startAngle,
      endAngle,
      margin: marginConfig,
      syncId,
      syncMethod,
      role,
      title,
      desc,
      id,
      className,
      style,
      tabIndex,
      layout,
      stackOffset,
      reverseStackOrder,
      barSize,
      barCategoryGap,
      barGap,
      maxBarSize,
      onClick,
      onMouseDown,
      onMouseUp,
      onMouseMove,
      onMouseEnter,
      onMouseLeave,
      onContextMenu,
      onDoubleClick,
      accessibilityLayer,
      children: [
        polarGridConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          import_recharts5.PolarGrid,
          {
            gridType: polarGridConfig.gridType,
            radialLines: polarGridConfig.radialLines,
            polarAngles: polarGridConfig.polarAngles,
            polarRadius: polarGridConfig.polarRadius,
            stroke: colors.comment,
            strokeOpacity: 0.3
          }
        ),
        polarAngleAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          import_recharts5.PolarAngleAxis,
          {
            dataKey: polarAngleAxisConfig.dataKey || angleKey,
            tick: polarAngleAxisConfig.tick ?? { fill: colors.text, fontSize: 12 },
            axisLine: polarAngleAxisConfig.axisLine ?? { stroke: colors.comment, strokeOpacity: 0.3 },
            axisLineType: polarAngleAxisConfig.axisLineType,
            orientation: polarAngleAxisConfig.orientation,
            ticks: polarAngleAxisConfig.ticks ? polarAngleAxisConfig.ticks.map((tick, index) => ({ ...tick, index })) : void 0
          }
        ),
        polarRadiusAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          import_recharts5.PolarRadiusAxis,
          {
            angle: polarRadiusAxisConfig.angle,
            domain: polarRadiusAxisConfig.domain,
            tick: polarRadiusAxisConfig.tick ?? { fill: colors.text, fontSize: 10 },
            axisLine: polarRadiusAxisConfig.axisLine ?? { stroke: colors.comment, strokeOpacity: 0.3 },
            orientation: polarRadiusAxisConfig.orientation,
            reversed: polarRadiusAxisConfig.reversed,
            ticks: polarRadiusAxisConfig.ticks ? polarRadiusAxisConfig.ticks.map((tick, index) => ({
              ...tick,
              index
            })) : void 0
          }
        ),
        tooltip?.show !== false && (() => {
          const { contentStyle, itemStyle, labelStyle, show, ...rest } = tooltip || {};
          return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
            import_recharts5.Tooltip,
            {
              ...rest,
              contentStyle: {
                backgroundColor: colors.background,
                border: `1px solid ${colors.text}`,
                borderRadius: theme.shape.borderRadius || 8,
                color: colors.text,
                boxShadow: "0 4px 6px -1px rgb(0 0 0 / 0.1)",
                ...contentStyle
              },
              itemStyle: {
                color: colors.text,
                fontSize: 12,
                ...itemStyle
              },
              labelStyle: {
                color: colors.text,
                fontWeight: 600,
                marginBottom: 4,
                ...labelStyle
              }
            }
          );
        })(),
        legendConfig.show && /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          import_recharts5.Legend,
          {
            align: legendConfig.align,
            verticalAlign: legendConfig.verticalAlign,
            layout: legendConfig.layout,
            iconType: legendConfig.iconType || "circle",
            formatter: (value) => /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("span", { style: { color: colors.text, fontSize: 13, fontWeight: 500 }, children: value }),
            wrapperStyle: { paddingTop: 20 }
          }
        ),
        radars?.map((radar, index) => /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          import_recharts5.Radar,
          {
            name: radar.name,
            dataKey: radar.dataKey,
            stroke: radar.stroke || radarColors[index % radarColors.length],
            strokeWidth: radar.strokeWidth ?? 2,
            strokeDasharray: radar.strokeDasharray,
            fill: radar.fill || radarColors[index % radarColors.length],
            fillOpacity: radar.fillOpacity ?? 0.6,
            hide: radar.hide,
            label: radar.label,
            dot: radar.dot ?? true,
            activeDot: radar.activeDot ?? true,
            connectNulls: radar.connectNulls,
            legendType: radar.legendType,
            tooltipType: radar.tooltipType,
            onMouseEnter: radar.onMouseEnter,
            onMouseLeave: radar.onMouseLeave,
            onClick: radar.onClick,
            isAnimationActive: radar.isAnimationActive,
            animationDuration: radar.animationDuration,
            animationBegin: radar.animationBegin,
            animationEasing: radar.animationEasing
          },
          `${radar.dataKey}-${index}`
        )),
        radialBars?.map((bar, index) => /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          import_recharts5.RadialBar,
          {
            name: bar.name,
            dataKey: bar.dataKey,
            fill: bar.fill || radarColors[index % radarColors.length],
            background: bar.background,
            cornerRadius: bar.cornerRadius,
            minPointSize: bar.minPointSize,
            maxBarSize: bar.maxBarSize,
            hide: bar.hide,
            label: bar.label,
            stackId: bar.stackId,
            isAnimationActive: bar.isAnimationActive,
            animationDuration: bar.animationDuration,
            animationBegin: bar.animationBegin,
            animationEasing: bar.animationEasing,
            onClick: bar.onClick,
            onMouseEnter: bar.onMouseEnter,
            onMouseLeave: bar.onMouseLeave
          },
          `${bar.dataKey}-${index}`
        )),
        children
      ]
    }
  ) });
};
RadarChart.displayName = "RadarChart";
var RadarChart_default = RadarChart;

// src/components/charts/RadialBarChart/RadialBarChart.tsx
var import_react6 = __toESM(require("react"), 1);
var import_oxygen_ui6 = require("@wso2/oxygen-ui");
var import_recharts6 = require("recharts");
var import_jsx_runtime6 = require("react/jsx-runtime");
var RadialBarChart = ({
  data,
  radialBars,
  height = 300,
  width = "100%",
  legend,
  tooltip,
  innerRadius = 0,
  outerRadius = "80%",
  cx = "50%",
  cy = "50%",
  startAngle = 0,
  endAngle = 360,
  margin,
  barSize,
  barCategoryGap,
  barGap,
  maxBarSize,
  polarGrid,
  polarAngleAxis,
  polarRadiusAxis,
  syncId,
  syncMethod,
  role,
  title,
  desc,
  id,
  className,
  style,
  tabIndex,
  stackOffset,
  reverseStackOrder,
  onClick,
  onMouseDown,
  onMouseUp,
  onMouseMove,
  onMouseEnter,
  onMouseLeave,
  onContextMenu,
  onDoubleClick,
  accessibilityLayer = true,
  colors: customColors,
  children
}) => {
  const theme = (0, import_oxygen_ui6.useTheme)();
  const { mode, systemMode } = (0, import_oxygen_ui6.useColorScheme)();
  const isDark = mode === "dark" || mode === "system" && systemMode === "dark";
  const colors = import_react6.default.useMemo(() => {
    const defaultColors = {
      primary: isDark ? "#F87643" : "#fa7b3f",
      background: isDark ? "#1e1e1e" : "#f5f5f5",
      text: isDark ? "#d4d4d4" : "#24292e",
      comment: isDark ? "#6a9955" : "#6a737d",
      keyword: isDark ? "#569cd6" : "#d73a49",
      string: isDark ? "#ce9178" : "#032f62",
      function: isDark ? "#dcdcaa" : "#6f42c1",
      number: isDark ? "#b5cea8" : "#005cc5",
      operator: isDark ? "#d4d4d4" : "#d73a49"
    };
    const themeSyntax = isDark ? theme.vars?.syntax?.dark : theme.vars?.syntax?.light;
    return {
      ...defaultColors,
      ...themeSyntax,
      primary: theme.palette.primary?.main || themeSyntax?.primary || defaultColors.primary
    };
  }, [isDark, theme.palette.primary?.main, theme.vars]);
  const barColors = import_react6.default.useMemo(() => {
    if (customColors && customColors.length > 0) return customColors;
    return [
      colors.primary,
      colors.keyword,
      colors.string,
      colors.function,
      colors.number,
      colors.operator
    ];
  }, [
    customColors,
    colors.primary,
    colors.keyword,
    colors.string,
    colors.function,
    colors.number,
    colors.operator
  ]);
  const legendConfig = import_react6.default.useMemo(
    () => ({ show: true, align: "center", verticalAlign: "bottom", ...legend }),
    [legend]
  );
  const marginConfig = import_react6.default.useMemo(
    () => ({ top: 20, right: 20, bottom: 20, left: 20, ...margin }),
    [margin]
  );
  const polarGridConfig = import_react6.default.useMemo(() => ({ show: false, ...polarGrid }), [polarGrid]);
  const polarAngleAxisConfig = import_react6.default.useMemo(
    () => ({ show: false, ...polarAngleAxis }),
    [polarAngleAxis]
  );
  const polarRadiusAxisConfig = import_react6.default.useMemo(
    () => ({ show: false, ...polarRadiusAxis }),
    [polarRadiusAxis]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(import_recharts6.ResponsiveContainer, { width, height, children: /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(
    import_recharts6.RadialBarChart,
    {
      data,
      cx,
      cy,
      innerRadius,
      outerRadius,
      startAngle,
      endAngle,
      margin: marginConfig,
      barSize,
      barCategoryGap,
      barGap,
      maxBarSize,
      syncId,
      syncMethod,
      role,
      title,
      desc,
      id,
      className,
      style,
      tabIndex,
      stackOffset,
      reverseStackOrder,
      onClick,
      onMouseDown,
      onMouseUp,
      onMouseMove,
      onMouseEnter,
      onMouseLeave,
      onContextMenu,
      onDoubleClick,
      accessibilityLayer,
      children: [
        polarGridConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
          import_recharts6.PolarGrid,
          {
            gridType: polarGridConfig.gridType,
            radialLines: polarGridConfig.radialLines,
            polarAngles: polarGridConfig.polarAngles,
            polarRadius: polarGridConfig.polarRadius,
            stroke: colors.comment,
            strokeOpacity: 0.3
          }
        ),
        polarAngleAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
          import_recharts6.PolarAngleAxis,
          {
            dataKey: polarAngleAxisConfig.dataKey,
            tick: polarAngleAxisConfig.tick ?? { fill: colors.text, fontSize: 12 },
            axisLine: polarAngleAxisConfig.axisLine ?? { stroke: colors.comment, strokeOpacity: 0.3 },
            axisLineType: polarAngleAxisConfig.axisLineType,
            orientation: polarAngleAxisConfig.orientation,
            ticks: polarAngleAxisConfig.ticks ? polarAngleAxisConfig.ticks.map((tick, index) => {
              if (typeof tick === "object" && tick !== null) {
                return { ...tick, index };
              }
              return tick;
            }) : void 0
          }
        ),
        polarRadiusAxisConfig?.show && /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
          import_recharts6.PolarRadiusAxis,
          {
            angle: polarRadiusAxisConfig.angle,
            domain: polarRadiusAxisConfig.domain,
            tick: polarRadiusAxisConfig.tick ?? { fill: colors.text, fontSize: 10 },
            axisLine: polarRadiusAxisConfig.axisLine ?? { stroke: colors.comment, strokeOpacity: 0.3 },
            orientation: polarRadiusAxisConfig.orientation,
            reversed: polarRadiusAxisConfig.reversed,
            ticks: polarRadiusAxisConfig.ticks ? polarRadiusAxisConfig.ticks.map((tick, index) => {
              if (typeof tick === "object" && tick !== null) {
                return { ...tick, index };
              }
              return tick;
            }) : void 0
          }
        ),
        tooltip?.show !== false && (() => {
          const { contentStyle, itemStyle, labelStyle, show, ...rest } = tooltip || {};
          return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
            import_recharts6.Tooltip,
            {
              ...rest,
              contentStyle: {
                backgroundColor: colors.background,
                border: `1px solid ${colors.text}`,
                borderRadius: theme.shape.borderRadius ?? 8,
                color: colors.text,
                boxShadow: "0 4px 6px -1px rgb(0 0 0 / 0.1)",
                ...contentStyle
              },
              itemStyle: {
                color: colors.text,
                fontSize: 12,
                ...itemStyle
              },
              labelStyle: {
                color: colors.text,
                fontWeight: 600,
                marginBottom: 4,
                ...labelStyle
              }
            }
          );
        })(),
        legendConfig.show && /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
          import_recharts6.Legend,
          {
            align: legendConfig.align,
            verticalAlign: legendConfig.verticalAlign,
            layout: legendConfig.layout,
            iconType: legendConfig.iconType || "circle",
            formatter: (value) => /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { style: { color: colors.text, fontSize: 13, fontWeight: 500 }, children: value }),
            wrapperStyle: { paddingTop: 20 }
          }
        ),
        radialBars?.map((bar, index) => /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
          import_recharts6.RadialBar,
          {
            name: bar.name,
            dataKey: bar.dataKey,
            fill: bar.fill || barColors[index % barColors.length],
            background: bar.background ?? { fill: colors.comment, opacity: 0.1 },
            cornerRadius: bar.cornerRadius,
            minPointSize: bar.minPointSize,
            maxBarSize: bar.maxBarSize,
            hide: bar.hide,
            label: bar.label,
            stackId: bar.stackId,
            legendType: bar.legendType,
            tooltipType: bar.tooltipType,
            isAnimationActive: bar.isAnimationActive,
            animationDuration: bar.animationDuration,
            animationBegin: bar.animationBegin,
            animationEasing: bar.animationEasing,
            onClick: bar.onClick,
            onMouseDown: bar.onMouseDown,
            onMouseUp: bar.onMouseUp,
            onMouseMove: bar.onMouseMove,
            onMouseOver: bar.onMouseOver,
            onMouseOut: bar.onMouseOut,
            onMouseEnter: bar.onMouseEnter,
            onMouseLeave: bar.onMouseLeave,
            onAnimationStart: bar.onAnimationStart,
            onAnimationEnd: bar.onAnimationEnd
          },
          `${bar.dataKey}-${index}`
        )),
        children
      ]
    }
  ) });
};
RadialBarChart.displayName = "RadialBarChart";
var RadialBarChart_default = RadialBarChart;

// src/index.ts
var import_recharts7 = require("recharts");
//# sourceMappingURL=index.cjs.map
