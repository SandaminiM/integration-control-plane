import { ReactElement } from 'react';
import { TableCellProps as MuiTableCellProps } from '@mui/material/TableCell';
export interface ListingTableCellProps extends MuiTableCellProps {
    /**
     * Truncate text with ellipsis when content overflows
     * @default false
     */
    truncate?: boolean;
    /**
     * Maximum width for the cell (useful with truncate)
     */
    maxWidth?: number | string;
}
export declare function ListingTableCell({ truncate, maxWidth, children, ...props }: ListingTableCellProps): ReactElement;
export default ListingTableCell;
