import { ReactElement } from 'react';
import { TableContainerProps as MuiTableContainerProps } from '@mui/material/TableContainer';
import { PaperProps } from '@mui/material/Paper';
export interface ListingTableContainerProps extends MuiTableContainerProps {
    /**
     * Elevation level for Paper wrapper
     * @default 1
     */
    elevation?: PaperProps['elevation'];
    /**
     * Remove Paper wrapper and render as plain container
     * @default false
     */
    disablePaper?: boolean;
}
export declare function ListingTableContainer({ elevation, disablePaper, children, ...props }: ListingTableContainerProps): ReactElement;
export default ListingTableContainer;
