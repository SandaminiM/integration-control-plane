import { ReactElement, ReactNode } from 'react';
import { ListingTableContextValue } from './context';
export interface ListingTableProviderProps extends ListingTableContextValue {
    /** Children to render within the provider */
    children: ReactNode;
}
/**
 * Provider component for ListingTable context.
 * Enables centralized state management and data operations across table sub-components.
 *
 * @example
 * ```tsx
 * <ListingTable.Provider
 *   searchValue={search}
 *   onSearchChange={setSearch}
 *   sortField={sortField}
 *   sortDirection={sortDirection}
 *   onSortChange={handleSortChange}
 * >
 *   <ListingTable.Container>
 *     <ListingTable.Toolbar showSearch />
 *     <ListingTable>...</ListingTable>
 *   </ListingTable.Container>
 * </ListingTable.Provider>
 * ```
 */
export declare function ListingTableProvider({ children, searchValue, onSearchChange, sortField, sortDirection, onSortChange, page, rowsPerPage, totalCount, onPageChange, onRowsPerPageChange, selected, onSelectionChange, onSelectAll, onClearSelection, isSelected, filters, onFilterChange, onClearFilters, onBulkDelete, onBulkAction, customHandlers, density, onDensityChange, variant, loading, }: ListingTableProviderProps): ReactElement;
export default ListingTableProvider;
