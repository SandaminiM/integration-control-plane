import { ReactElement, ReactNode } from 'react';
import { SxProps, Theme } from '@mui/material/styles';
export interface ListingTableEmptyStateProps {
    /**
     * Main title text
     * @default 'No data available'
     */
    title?: string;
    /**
     * Description text below the title
     */
    description?: string;
    /**
     * Custom illustration element (e.g., icon or image)
     */
    illustration?: ReactNode;
    /**
     * Action button or link element
     */
    action?: ReactNode;
    /**
     * Minimum height of the empty state container
     * @default 300
     */
    minHeight?: number | string;
    /**
     * Custom styles for the container
     */
    sx?: SxProps<Theme>;
}
export declare function ListingTableEmptyState({ title, description, illustration, action, minHeight, sx, }: ListingTableEmptyStateProps): ReactElement;
export default ListingTableEmptyState;
